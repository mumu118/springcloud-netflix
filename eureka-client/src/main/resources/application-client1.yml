server:
  port: 1107

eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/,http://localhost:2222/eureka/
spring:
  application:
    name: eureka-client
